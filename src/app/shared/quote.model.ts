export class Quote {

    // public quote: string;
    // public answer: string;

    // constructor(quote: string, answer: string){
    //     this.quote = quote;
    //     this.answer = answer;
    // }

    constructor(public quote: string, public answer: string) { }
}